---
description: Load AI-optimized session context
---

Run `thrum prime` to gather identity, team, inbox, git context, and daemon health.

```bash
thrum prime
```

Use `thrum prime --json` for structured output.
