{
  "name": "thrum-e2e",
  "version": "0.3.0",
  "private": true,
  "description": "E2E tests for Thrum agent coordination system",
  "scripts": {
    "test:e2e": "npx playwright test",
    "test:e2e:headed": "npx playwright test --headed",
    "test:e2e:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
