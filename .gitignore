# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/thrum

# Macos stuff
.DS_Store

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Development artifacts (local only, not for public repo)
dev-docs/
docs/plans/
docs/superpowers/
.agents/
!codex-plugin/.agents/
!/.agents/
/.agents/*
!/.agents/plugins/
/.agents/plugins/*
!/.agents/plugins/marketplace.json
.beads/
.beads_backup/
.claude/

# Editor/IDE
# .idea/
# .vscode/

# Reference repos (for development, not tracked)
.ref/

# Build outputs
bin/
dist/
output/
build/
.next/
.turbo/

# Embedded UI build artifacts (override blanket dist/ ignore above)
!internal/web/dist/
internal/web/dist/*
!internal/web/dist/.gitkeep

.playwright-mcp/
.cursor/

# Node.js / Playwright
node_modules/
package-lock.json
!opencode-plugin/package-lock.json
test-results/
playwright-report/

# Screenshots (Playwright testing artifacts)
*.png
!website/img/**/*.png

# Playwright cli artifacts
.playwright-cli

# Open Code plugin build artifacts
opencode-plugin/dist/
opencode-plugin/node_modules/

# Thrum runtime files
.thrum/
.thrum.*.json

# Thrum data directory (all data lives on a-sync branch via worktree)
.thrum/
.thrum.*.json
scripts/thrum-startup.sh

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads / Dolt files (added by bd init)
.beads-credential-key

# Safety snapshot from bd 0.62 → 1.0 embedded migration (delete once verified)
.beads.server-backup/

# Stale role-template location (moved to internal/context/roleconfig/templates/
# in thrum-z2et.20.1). Some tooling may recreate the directory; ignore it so
# the no-frontmatter copies don't surface as untracked drift.
toolkit/templates/roles/
